<!DOCTYPE html>
<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:w="urn:schemas-microsoft-com:office:word" xmlns="http://www.w3.org/TR/REC-html40">
<head>
<meta charset="utf-8">
<title>JavaWeb电商系统 - 期末答辩准备文档</title>
<style>
body { font-family: "Microsoft YaHei", SimSun, Arial; font-size: 12pt; line-height: 2; margin: 60px; color: #333; }
h1 { font-size: 20pt; color: #1a5276; border-bottom: 3px solid #1a5276; padding-bottom: 10px; margin-top: 30px; }
h2 { font-size: 15pt; color: #2874a6; margin-top: 20px; background: #eaf2f8; padding: 8px 15px; border-left: 5px solid #2874a6; }
h3 { font-size: 13pt; color: #1a5276; margin-top: 15px; }
p { margin: 6px 0; }
table { border-collapse: collapse; width: 100%; margin: 12px 0; font-size: 11pt; }
th { background: #2874a6; color: white; padding: 10px 12px; text-align: left; }
td { border: 1px solid #ddd; padding: 8px 12px; }
tr:nth-child(even) { background: #f8f9fa; }
.box { background: #fef9e7; border: 2px solid #f39c12; border-radius: 8px; padding: 15px 20px; margin: 12px 0; }
.green-box { background: #eafaf1; border: 2px solid #27ae60; border-radius: 8px; padding: 15px 20px; margin: 12px 0; }
.red-box { background: #fdedec; border: 2px solid #e74c3c; border-radius: 8px; padding: 15px 20px; margin: 12px 0; }
.blue-box { background: #eaf2f8; border: 2px solid #3498db; border-radius: 8px; padding: 15px 20px; margin: 12px 0; }
.page-break { page-break-before: always; }
.big-text { font-size: 14pt; }
.flow { background: #f5f5f5; padding: 15px 20px; border-radius: 8px; margin: 10px 0; line-height: 2.2; }
ul { margin: 6px 0; }
li { margin: 4px 0; }
</style>
</head>
<body>

<h1 style="text-align:center; border:none; font-size: 26pt; color: #1a5276;">🛒 电商系统 · 期末答辩准备</h1>
<p style="text-align:center; font-size: 14pt; color: #666;">6人小组版 · 零基础友好 · 通俗易懂</p>

<!-- ============ 第一部分 ============ -->
<h1>一、我们的项目是什么？</h1>

<p class="big-text">简单说：我们做了一个<strong>网上商城</strong>，类似于一个简化版的淘宝。</p>

<p>用户可以在上面<strong>浏览商品、加入购物车、下单付款、写评价</strong>；管理员可以在后台<strong>管理商品、处理订单、管理评价</strong>。</p>

<h2>系统有两种角色</h2>
<table>
<tr><th>角色</th><th>能干什么</th><th>登录后看到什么</th></tr>
<tr><td>👤 普通顾客</td><td>逛商城、加购物车、下单、评价商品</td><td>商城首页（shop.jsp）</td></tr>
<tr><td>👑 管理员</td><td>管理商品、管理订单、管理评价</td><td>后台管理页（admin.jsp）</td></tr>
</table>

<div class="box">
<strong>📝 演示用的测试账号：</strong>
<table>
<tr><th>用户名</th><th>密码</th><th>角色</th></tr>
<tr><td>admin</td><td>123456</td><td>普通顾客</td></tr>
<tr><td>admin2</td><td>123456</td><td>管理员</td></tr>
<tr><td>user3</td><td>123456</td><td>普通顾客</td></tr>
</table>
</div>

<h2>六大功能一览</h2>
<div class="green-box">
<ol>
<li><strong>登录/注册/退出</strong> — 用户身份管理</li>
<li><strong>商品浏览</strong> — 10大分类、30个商品、可以搜索</li>
<li><strong>购物车</strong> — 加商品、改数量、删除</li>
<li><strong>下单</strong> — 购物车一键结算，生成订单</li>
<li><strong>评价</strong> — 买过的商品可以打分+写评价</li>
<li><strong>后台管理</strong> — 管理员管理商品、订单、评价</li>
</ol>
</div>

<!-- ============ 第二部分 ============ -->
<div class="page-break"></div>
<h1>二、用了什么技术？（技术栈）</h1>

<p>用大白话解释我们用的每个技术：</p>

<table>
<tr><th>技术</th><th>大白话解释</th></tr>
<tr><td><strong>Java</strong></td><td>编程语言，写后端逻辑的</td></tr>
<tr><td><strong>Servlet</strong></td><td>Java里处理网页请求的"程序"，相当于一个"服务员"，接收用户的请求，处理完返回结果</td></tr>
<tr><td><strong>JSP</strong></td><td>就是网页页面，但可以在HTML里嵌入Java代码，类似于PHP</td></tr>
<tr><td><strong>MySQL</strong></td><td>数据库，存储所有数据的地方（用户信息、商品、订单等）</td></tr>
<tr><td><strong>JDBC</strong></td><td>Java连接数据库的"桥梁"，让Java代码能读写MySQL里的数据</td></tr>
<tr><td><strong>Tomcat</strong></td><td>Web服务器，运行我们Java程序的"容器"，没有它网页打不开</td></tr>
<tr><td><strong>Session</strong></td><td>"会话"，用来记住用户登录状态，不然每次点击都要重新登录</td></tr>
</table>

<div class="blue-box">
<strong>💡 答辩时怎么说：</strong><br>
"我们用的是纯原生 Java Web 技术，没有用 Spring 这样的框架，这样更能体现我们对底层原理的理解。"
</div>

<!-- ============ 第三部分 ============ -->
<div class="page-break"></div>
<h1>三、代码是怎么组织的？（项目结构）</h1>

<h2>三层架构（MVC）</h2>

<p>我们的代码按照<strong>三层结构</strong>来组织，就像餐厅里的分工：</p>

<div class="flow">
<table>
<tr><th>层</th><th>比喻</th><th>我们项目里对应的东西</th></tr>
<tr><td><strong>View（视图层）</strong></td><td>🍽️ 餐厅的"大堂"<br>顾客看到的地方</td><td>JSP 页面（login.jsp、shop.jsp 等）<br>负责展示界面给用户看</td></tr>
<tr><td><strong>Controller（控制层）</strong></td><td>👨‍🍳 餐厅的"服务员"<br>接收顾客的需求，传给后厨</td><td>Servlet 类（LoginServlet、CartServlet 等）<br>接收用户请求，决定调谁处理</td></tr>
<tr><td><strong>Model + DAO（数据层）</strong></td><td>🏭 餐厅的"后厨"<br>真正干活的地方</td><td>Model 类 + DAO 类<br>负责查数据库、写数据库</td></tr>
</table>
</div>

<h2>文件夹说明</h2>
<table>
<tr><th>文件夹</th><th>里面放什么</th><th>举个例子</th></tr>
<tr><td>model/</td><td>数据模型 — 描述数据长什么样</td><td>User.java（用户有用户名、密码、角色）</td></tr>
<tr><td>dao/</td><td>数据操作 — 和数据库打交道的代码</td><td>UserDAO.java（查用户、注册用户的SQL都在这）</td></tr>
<tr><td>servlet/</td><td>控制器 — 处理网页请求</td><td>LoginServlet.java（处理登录请求）</td></tr>
<tr><td>util/</td><td>工具类 — 公共的基础功能</td><td>DBUtil.java（获取数据库连接）</td></tr>
<tr><td>webapp/</td><td>网页文件</td><td>login.jsp、shop.jsp 等页面</td></tr>
</table>

<h2>一个请求的完整过程</h2>
<div class="flow">
<strong>以"用户点击登录"为例：</strong><br><br>
① 用户在浏览器输入用户名密码，点击"登录"<br>
② 浏览器把数据发给服务器（Tomcat）<br>
③ Tomcat 找到 LoginServlet（因为它的注解是 @WebServlet("/login")）<br>
④ LoginServlet 调用 UserDAO.authenticate() 去数据库查这个用户<br>
⑤ UserDAO 用 JDBC 执行 SQL：SELECT * FROM users WHERE username=? AND password=?<br>
⑥ 查到了 → 创建 Session 记住登录状态 → 跳转到商城页面<br>
⑦ 查不到 → 返回登录页面，显示"用户名或密码错误"
</div>

<!-- ============ 第四部分 ============ -->
<div class="page-break"></div>
<h1>四、数据库设计（6张表）</h1>

<p>数据库就像一个大表格柜子，我们把不同类型的数据存在不同的"抽屉"（表）里：</p>

<h2>4.1 表一：users（用户表）</h2>
<p>存所有用户的信息。</p>
<table>
<tr><th>字段</th><th>含义</th><th>举例</th></tr>
<tr><td>username</td><td>用户名（唯一）</td><td>admin、user3</td></tr>
<tr><td>password</td><td>密码</td><td>123456</td></tr>
<tr><td>role</td><td>角色</td><td>customer（顾客） 或 admin（管理员）</td></tr>
</table>

<h2>4.2 表二：products（商品表）</h2>
<p>存所有商品信息，共 30 个商品，分 10 个大类。</p>
<table>
<tr><th>字段</th><th>含义</th><th>举例</th></tr>
<tr><td>id</td><td>商品编号</td><td>P001、C001</td></tr>
<tr><td>name</td><td>商品名</td><td>智能手机、笔记本电脑</td></tr>
<tr><td>category</td><td>分类</td><td>phone（手机）、computer（电脑）</td></tr>
<tr><td>icon</td><td>图标（emoji）</td><td>📱、💻、🛋️</td></tr>
<tr><td>price</td><td>价格</td><td>2999.00</td></tr>
</table>

<p><strong>10大分类：</strong>手机数码、电脑办公、家居家装、服饰鞋包、食品饮料、图书文具、运动户外、美妆个护、母婴玩具、五金工具</p>

<h2>4.3 表三：cart（购物车表）</h2>
<p>存每个用户购物车里的商品。</p>
<table>
<tr><th>字段</th><th>含义</th><th>举例</th></tr>
<tr><td>username</td><td>谁的购物车</td><td>admin</td></tr>
<tr><td>product_id</td><td>哪个商品</td><td>P001</td></tr>
<tr><td>quantity</td><td>几个</td><td>2</td></tr>
</table>
<p><strong>特殊规则</strong>：同一个用户不能重复加同一个商品（联合唯一约束），如果重复加，数量会自动累加。</p>

<h2>4.4 表四：orders（订单表）</h2>
<p>存所有订单的基本信息。</p>
<table>
<tr><th>字段</th><th>含义</th><th>举例</th></tr>
<tr><td>id</td><td>订单号（时间戳生成）</td><td>ORD1779352557157</td></tr>
<tr><td>username</td><td>谁下的单</td><td>admin</td></tr>
<tr><td>total_amount</td><td>总金额</td><td>3446.00</td></tr>
<tr><td>status</td><td>订单状态</td><td>待发货 → 已发货 → 已完成</td></tr>
</table>

<h2>4.5 表五：order_items（订单详情表）</h2>
<p>一个订单里有哪些商品（一个订单可以买多个商品，所以单独一张表）。</p>
<table>
<tr><th>字段</th><th>含义</th><th>举例</th></tr>
<tr><td>order_id</td><td>属于哪个订单</td><td>ORD1779352557157</td></tr>
<tr><td>product_name</td><td>商品名</td><td>智能手机</td></tr>
<tr><td>price</td><td>买的时候多少钱</td><td>2999.00</td></tr>
<tr><td>quantity</td><td>买了几个</td><td>1</td></tr>
</table>

<h2>4.6 表六：product_reviews（评价表）</h2>
<p>存用户对商品的评价。</p>
<table>
<tr><th>字段</th><th>含义</th><th>举例</th></tr>
<tr><td>product_id</td><td>评价哪个商品</td><td>P001</td></tr>
<tr><td>username</td><td>谁评价的</td><td>admin</td></tr>
<tr><td>rating</td><td>打几分</td><td>5（满分5星）</td></tr>
<tr><td>content</td><td>评价内容</td><td>非常好用，推荐购买！</td></tr>
</table>

<div class="box">
<strong>📐 表和表之间的关系：</strong>
<ul>
<li>一个用户可以有<strong>多个</strong>订单</li>
<li>一个订单包含<strong>多个</strong>订单详情（因为可以买多个商品）</li>
<li>一个商品可以有<strong>多条</strong>评价</li>
<li>删除订单时，对应的订单详情会<strong>自动删除</strong>（这叫"级联删除"）</li>
</ul>
</div>

<!-- ============ 第五部分 ============ -->
<div class="page-break"></div>
<h1>五、核心功能怎么实现的？</h1>

<h2>5.1 登录功能</h2>

<div class="flow">
<strong>用户视角：</strong>输入用户名密码 → 点击登录 → 成功就进入商城<br><br>
<strong>背后发生了什么：</strong><br>
① 页面把用户名密码发给 LoginServlet<br>
② LoginServlet 调用 UserDAO 去数据库查：这个用户名+密码对不对？<br>
③ 对上了 → 在 Session 里记住"这个人已经登录了"<br>
④ 根据角色跳转：顾客去商城，管理员去后台
</div>

<div class="blue-box">
<strong>💡 什么是 Session？</strong><br>
网页和网页之间是"不认识"的。Session 就像给你发了一张"临时身份证"，上面写着你的名字和角色。只要你拿着这张身份证（30分钟内），系统就知道你是谁，不用每次都重新登录。
</div>

<h2>5.2 商品浏览和搜索</h2>

<div class="flow">
<strong>分类浏览：</strong>点击左边分类 → JavaScript 发请求到后端 → 后端查数据库 → 返回 JSON 数据 → 前端显示商品卡片<br><br>
<strong>搜索商品：</strong>输入关键词 → 后端用 SQL 的 LIKE 模糊匹配 → 返回匹配的商品<br><br>
<strong>亮点：</strong>切换分类时<strong>页面不刷新</strong>，只用 JavaScript 局部更新，用户体验更好（这叫 AJAX 技术）
</div>

<h2>5.3 购物车</h2>

<div class="flow">
<strong>加入购物车：</strong>点击"加入购物车" → 后端把商品写入 cart 表<br>
<strong>重复添加：</strong>如果购物车里已经有了，不重复插入，而是<strong>数量+1</strong><br>
<strong>修改数量：</strong>直接更新 cart 表里的 quantity 字段<br>
<strong>删除商品：</strong>从 cart 表里删除对应记录<br>
<strong>清空购物车：</strong>删除该用户的所有购物车记录
</div>

<div class="box">
<strong>🔒 权限控制：</strong>购物车只有普通顾客能用。管理员访问购物车会返回 403 错误（权限不足）。
</div>

<h2>5.4 下单（重点！）</h2>

<div class="flow">
<strong>流程：</strong>点击结算 → 读取购物车所有商品 → 计算总价 → 创建订单 → 清空购物车<br><br>
<strong>为什么要用"事务"？</strong><br>
下单要同时做好几件事：① 插入订单 ② 插入订单详情 ③ 清空购物车<br>
如果做到一半失败了（比如①成功了但②失败了），就会产生"空订单"这种错误数据。<br><br>
<strong>解决方案：数据库事务</strong><br>
把这几步"打包"成一个整体：要么全部成功（commit），要么全部撤销（rollback），不会出现做了一半的情况。
</div>

<div class="blue-box">
<strong>💡 答辩时可以这样比喻：</strong><br>
"事务就像银行转账，A 账户扣钱和 B 账户加钱必须同时成功，如果只扣了钱没加上，那就出大问题了。事务保证这两步要么都完成，要么都不做。"
</div>

<h2>5.5 评价功能</h2>

<div class="flow">
<strong>规则：</strong>只有<strong>买过这个商品</strong>的用户才能评价（防止刷评）<br><br>
<strong>怎么判断买没买过？</strong><br>
后端去订单表（orders）和订单详情表（order_items）里查：这个用户有没有买过这个商品。如果查到了，就允许评价；查不到，就拒绝。<br><br>
<strong>评价内容：</strong>1-5 星评分 + 文字评价<br>
<strong>管理员权限：</strong>管理员可以删除不当评价
</div>

<h2>5.6 自动初始化数据库（项目亮点）</h2>

<div class="green-box">
<strong>这个功能的好处：</strong>项目启动时<strong>自动</strong>创建数据库、创建表、插入初始数据，不需要手动导入 SQL 文件。<br><br>
<strong>智能判断：</strong>如果数据库已经存在、表已经存在、数据已经有了，就跳过，不会重复创建或报错。<br><br>
<strong>答辩时怎么说：</strong><br>
"我们实现了数据库的自动初始化，项目第一次启动时会自动建库建表，而且具有幂等性——重复启动不会报错也不会重复插入数据，方便了部署和迁移。"
</div>

<!-- ============ 第六部分 ============ -->
<div class="page-break"></div>
<h1>六、答辩分工建议（6人）</h1>

<div class="box">
<p>建议每人负责一个模块，答辩时<strong>各讲各的部分</strong>，这样每个人都有事做：</p>
</div>

<table>
<tr><th>同学</th><th>负责讲什么</th><th>需要准备的内容</th></tr>
<tr><td><strong>A</strong></td><td>项目介绍 + 技术架构</td><td>项目是做什么的、用了什么技术、代码怎么组织的</td></tr>
<tr><td><strong>B</strong></td><td>数据库设计</td><td>6张表的设计、表关系、自动初始化功能</td></tr>
<tr><td><strong>C</strong></td><td>登录/注册/权限</td><td>演示登录注册、讲解 Session、角色权限控制</td></tr>
<tr><td><strong>D</strong></td><td>商品 + 购物车</td><td>演示商品浏览/搜索、购物车操作、AJAX不刷新</td></tr>
<tr><td><strong>E</strong></td><td>订单 + 评价</td><td>演示下单流程、讲解事务、演示评价功能</td></tr>
<tr><td><strong>F</strong></td><td>管理后台 + 总结</td><td>演示后台管理、项目亮点和不足、回答提问</td></tr>
</table>

<!-- ============ 第七部分 ============ -->
<div class="page-break"></div>
<h1>七、老师可能问的问题（附答案）</h1>

<h2>Q1：为什么用 Servlet 不用 Spring Boot？</h2>
<div class="green-box">
这是学习项目，用原生 Servlet 能更好地理解底层原理。掌握了 Servlet 之后再去学 Spring 会更轻松，因为 Spring 底层也是 Servlet。
</div>

<h2>Q2：Session 是什么？</h2>
<div class="green-box">
Session 是服务器用来记住用户状态的机制。用户登录后，服务器创建一个 Session 存储用户信息，后续请求都带着这个 Session，就不用每次重新登录了。我们设置 30 分钟过期。
</div>

<h2>Q3：什么是 SQL 注入？你们怎么防的？</h2>
<div class="green-box">
SQL 注入是攻击者在输入框输入恶意代码来破坏数据库。我们用 <strong>PreparedStatement</strong>（预编译语句）来防止，它会把用户输入当作纯文本处理，不会被当成 SQL 命令执行。
</div>

<h2>Q4：什么是数据库事务？哪里用了？</h2>
<div class="green-box">
事务保证一组操作要么全成功，要么全失败。我们在<strong>创建订单</strong>时用了事务：插入订单+插入详情+清空购物车，如果中间出错，前面的操作会自动撤销。
</div>

<h2>Q5：密码是明文存储的，有什么问题？</h2>
<div class="green-box">
确实不安全，如果数据库被泄露密码就全暴露了。实际项目应该用加密算法（如 BCrypt）存储密码的哈希值。这是我们可以改进的地方。
</div>

<h2>Q6：MVC 是什么？</h2>
<div class="green-box">
MVC 是三层架构：Model（数据模型）、View（页面展示）、Controller（请求处理）。好处是分工明确，改页面不用改业务代码，改数据库不用改页面。
</div>

<h2>Q7：你们怎么做到页面不刷新就更新内容的？</h2>
<div class="green-box">
用了 AJAX 技术。前端用 JavaScript 的 fetch() 函数悄悄向后端发请求，拿到数据后用 JavaScript 修改页面内容，整个过程不刷新页面，用户体验更好。
</div>

<h2>Q8：项目有什么不足？</h2>
<div class="green-box">
① 密码明文存储，应该加密<br>
② 没有分页功能，商品多了会卡<br>
③ JSON 是手动拼接的，应该用专门的库（如 Gson）<br>
④ 没有用数据库连接池，每次查询都新建连接，效率不高<br>
⑤ 没有用 Maven 管理依赖，不够规范
</div>

<h2>Q9：为什么管理员不能用购物车？</h2>
<div class="green-box">
这是业务设计上的考虑。管理员是负责管理后台的，不参与购物。购物车和下单功能只对普通顾客开放，这样职责更清晰。代码里通过检查 Session 中的角色字段来实现这个限制。
</div>

<h2>Q10：外键和级联删除是什么意思？</h2>
<div class="green-box">
外键是表之间的关联。比如订单详情的 order_id 指向订单表的 id，表示"这个详情属于哪个订单"。级联删除是指：删除订单时，关联的订单详情会自动删除，不用手动删，避免出现"孤儿数据"。
</div>

<!-- ============ 第八部分 ============ -->
<div class="page-break"></div>
<h1>八、演示流程建议</h1>

<div class="green-box">
<strong>🎬 推荐演示顺序（10-15分钟）：</strong>
</div>

<table>
<tr><th>步骤</th><th>谁来做</th><th>做什么</th></tr>
<tr><td>①</td><td>同学A</td><td>打开项目，展示目录结构，简单说下用了什么技术</td></tr>
<tr><td>②</td><td>同学A</td><td>启动 Tomcat，看控制台输出"数据库初始化完成"</td></tr>
<tr><td>③</td><td>同学C</td><td>打开浏览器，用 admin/123456 登录（普通顾客身份）</td></tr>
<tr><td>④</td><td>同学D</td><td>切换商品分类、搜索商品、点击商品看详情</td></tr>
<tr><td>⑤</td><td>同学D</td><td>加几个商品到购物车，进入购物车页面</td></tr>
<tr><td>⑥</td><td>同学E</td><td>点击结算下单，展示订单列表</td></tr>
<tr><td>⑦</td><td>同学E</td><td>进入已购商品详情页，提交评价</td></tr>
<tr><td>⑧</td><td>同学F</td><td>退出登录，用 admin2/123456 登录（管理员身份）</td></tr>
<tr><td>⑨</td><td>同学F</td><td>展示后台：商品管理、订单发货、评价管理</td></tr>
</table>

<div class="red-box">
<strong>⚠️ 演示前的检查清单：</strong>
<ul>
<li>✅ MySQL 服务已启动</li>
<li>✅ Tomcat 配置正确，能正常运行</li>
<li>✅ 提前测试一遍完整流程，确保没有报错</li>
<li>✅ 准备好两个账号（顾客+管理员），避免演示时来回切换出错</li>
</ul>
</div>

<div class="blue-box">
<strong>💡 答辩小技巧：</strong>
<ul>
<li>老师提问时，先<strong>认真听完</strong>再回答，不要抢话</li>
<li>不确定的问题可以说："这个我们课后可以进一步研究"</li>
<li>主动说出项目不足（密码明文、没分页等），<strong>老师会觉得你有思考</strong></li>
<li>演示时如果出错<strong>不要慌</strong>，看看控制台日志找原因</li>
</ul>
</div>

<hr style="margin: 40px 0;">
<p style="text-align:center; color: #999;">— 文档结束 · 祝答辩顺利！ —</p>

</body>
</html>